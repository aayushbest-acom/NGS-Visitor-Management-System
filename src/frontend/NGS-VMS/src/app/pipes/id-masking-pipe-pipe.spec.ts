import { IdMaskingPipePipe } from './id-masking-pipe-pipe';

describe('IdMaskingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new IdMaskingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
